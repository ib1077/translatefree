# 列車番号の固定配置

1列車1ラベル。時刻データを読み込んだとき、全日図を基準に配置する区間とその区間内の位置を決定します。スクロール、ピンチ、表示方向の絞り込み、列車選択、画面回転では決め直しません。画面外へ出たラベルを画面端や別区間へ移す処理はありません。

始発寄りの走行区間を優先し、上りは線の左側、下りは右側へ置き、線と同じ角度で表示します。停車の水平部分は通常の候補から除外します。特急を先に配置し、列車線との衝突、ラベル同士の衝突を減らすため、必要に応じて先の区間へ移します。文字は普通7.5–8px、特急9–9.5pxの候補から選びます。列車線を白く消す縁取りは使いません。

固定するのは列車線上の取り付け位置です。拡大で線の傾きが変わると文字の角度も追従し、線との余白を保ちます。画面幅に応じて文字と余白を基準の90〜105%の範囲で穏やかに調整します。画面上の絶対位置を固定するものではありません。

画面は横長スマホの全日図、印刷はA4横の全日図をそれぞれ一定サイズの基準として配置します。現在の端末幅に合わせて候補を選び直さないため、どの端末でも同じ取り付け位置になります。ただし、取り付け区間を安定させる都合上、あらゆる画面比率・倍率で重なりがなくなる保証はありません。混雑箇所は列車別の補正で調整できます。

## 個別の補正

元時刻データは変更せず、`ui-config.js` の `labelOverrides.screen` または `labelOverrides.print` に列車番号をキーとして追加します。両者は独立です。

```js
labelOverrides: {
  screen: {'724D': {segment: 2, fraction: 0.55, fontSize: 9, offset: 8}},
  print: {}
}
```

`segment` はpoints配列の0始まりの区間番号、`fraction` は区間内の0〜1の位置、`fontSize` は文字サイズ、`offset` は線との中心間距離（px）です。値は例示で、現在は個別補正を登録していません。組み込み時はcreateViewerの `labelOverrides` 引数でも同じ構造を渡せます。ダイヤ差し替えで点の並びを変更する場合は、登録済みの補正区間も確認してください。

変更後は `tools/build.py` で単独HTMLとPWAを同時に再生成します。ラベル配置は `label-layout.js`、描画と切り抜きは `diagram-view.js` に分離しています。
